/*
    Plugin-SDK (Grand Theft Auto San Andreas) source file
    Authors: GTA Community. See more here
    https://github.com/DK22Pac/plugin-sdk
    Do not delete this comment block. Respect others' work!
*/


/*
* 
* DOESN'T EXIST ON PC
* 
*/

/*
#include "CEventPotentialWalkIntoBuilding.h"


CEventPotentialWalkIntoBuilding::CEventPotentialWalkIntoBuilding(const CBuilding* pBuilding, const CVector& vNormal, const CVector& vPos, const int iMoveState)
    : CEventBuildingCollision(plugin::dummy) {
    plugin::CallMethod<(uintptr_t)nullptr, CEventPotentialWalkIntoBuilding*, const CBuilding*, const CVector&, const CVector&, const int>
        (this, pBuilding, vNormal, vPos, iMoveState);
}

eEventType CEventPotentialWalkIntoBuilding::GetEventType() {
    return ((eEventType(__thiscall*)(CEventPotentialWalkIntoBuilding*))plugin::GetVMT(this, 1))(this);
}

int CEventPotentialWalkIntoBuilding::GetEventPriority() {
    return ((int(__thiscall*)(CEventPotentialWalkIntoBuilding*))plugin::GetVMT(this, 2))(this);
}

CEvent* CEventPotentialWalkIntoBuilding::Clone() {
    return ((CEvent * (__thiscall*)(CEventPotentialWalkIntoBuilding*))plugin::GetVMT(this, 4))(this);
}

bool CEventPotentialWalkIntoBuilding::AffectsPed(CPed* pPed) {
    return ((bool(__thiscall*)(CEventPotentialWalkIntoBuilding*, CPed*))plugin::GetVMT(this, 5))(this, pPed);
}
*/